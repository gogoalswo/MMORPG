node_modules/
# SQLite DB (계정·캐릭터). 절대 커밋하지 않는다
packages/server/data/
dist/
.env
.env.local
*.log
.DS_Store
# 원본 에셋은 커밋하지 않는다 — scripts/fetch-assets.sh 로 다시 받는다
assets-src/*.zip
assets-src/*.hdr
assets-src/*_1K-JPG/
assets-src/textures/
assets-src/models/
# 검증·서버 로그
logs/
# 코드가 읽는 건 sky_1k.hdr 뿐이다. 2k 는 fetch 스크립트가 받아만 두고
# 아무도 안 쓴다 — 5.4MB 를 이력에 영구히 남길 이유가 없다.
public/assets/hdri/sky_2k.hdr
# 고도 — 임포트 캐시와 빌드 산출물. 엔진 바이너리도 저장소에 넣지 않는다
# (받는 방법은 docs/features/godot-migration.md).
godot/.godot/
godot/android/
*.apk
*.aab
*.keystore
