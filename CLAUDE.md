# 이 저장소에서 일하는 방식

## 지금 무엇을 만들고 있나 ★★

**고도(Godot) 엔진으로 만든다. 목표는 모바일 앱이다.** 게임은 `godot/` 에만 있다 —
클라이언트가 한 벌뿐이라 어느 쪽을 고칠지 헷갈릴 일이 없다.

| | |
|---|---|
| `godot/` | **지금 만드는 것.** 판정(`world/`) · 화면(`game/`) · 통로(`net/`) · 테스트(`tests/`) |
| `packages/shared` | **수치와 생성기.** 존·몬스터·아이템·스킬을 여기서 만든다. 고치면 `npm run export:godot` |
| `packages/server` | 옛 Colyseus 서버. **판정 원본**이라 남겨 뒀다 — 고도 서버를 붙일 때 대조한다 |

옛 three.js 웹 클라이언트(`packages/client`)는 **2026-09-17 에 지웠다** (요청:
"웹페이지 관련된 내용을 삭제해. 그럼 헷갈릴 일도 없어"). `docs/features/` 의 옛
문서에 `packages/client/...` 경로가 남아 있는데 **이름 단서로만 읽는다.**

무엇이 옮겨졌고 왜 그렇게 했는지는 **[`docs/features/godot-migration.md`](docs/features/godot-migration.md)** 에
있다. 고도 쪽 일을 시작하면 **이것부터 읽는다.**

고도 작업 한 바퀴:

```bash
npm run sync:godot     # 에셋을 godot/assets 로 (모델은 텍스처를 512 로 줄여서)
npm run export:godot   # shared 표를 godot/data/*.json 으로
npm run test:godot     # 고도 테스트 — 통과는 한 줄, 실패만 자세히
```

블렌더도 설치 없이 쓴다 — `npm run blender -- --python 스크립트.py` (없으면 알아서
받는다) → [`docs/features/blender.md`](docs/features/blender.md).
**단, 사용자가 애니메이션을 만들어 달라고 할 때만 쓴다** (아래 규칙 참고).

화면은 `https://gogoalswo.github.io/MMORPG/` 에서 본다. 밀고 나면
**"배포됐습니다, 새로고침하세요" 와 빌드 표시**(`빌드 <커밋> <시각>`)를 알려 준다 —
브라우저가 최대 10분 캐시해서 그게 없으면 갱신됐는지 알 수 없다.

## 사용자와 일하는 규칙 ★★

어느 PC·클라우드 세션에서 열어도 같게 지키도록 여기에 적는다. 전부 실제로 지적받은 것이다.

- **답은 한국어로만 쓴다.** 답변·중간 보고·표·질문, 그리고 **도구 호출 사이의 한두 줄
  진행 설명까지** 예외 없다. 코드 식별자·파일명·명령어처럼 원래 영어인 것만 그대로 둔다.
  (영어로 답했다가 "다시는 내 앞에서 영어 사용하지마라" 는 지적을 받았고, 긴 작업 중
  진행 설명이 영어로 새서 또 지적받았다.)
- **스크린샷은 사용자가 요청할 때만 찍는다.** UI·수치 확인은 글로 한다 (DOM 읽기,
  `npm run probe`, `logs/`). 사람이 눈으로 봐야 하는 단계면 "확인해 보시겠어요?" 라고
  묻고 기다린다.
- **이펙트만은 예외다 — 내가 직접 뽑아 본다** (2026-09-18 지시: "스크린샷 보는
  방향으로 바꿀까?"). ★ `npm run shot:godot` 으로 게임을 가상 디스플레이에 띄워
  찍는다. 낙뢰를 노드 수치로만 확인했다가 **"퀄리티가 너무 떨어진다"** 는 지적을
  받았다 — 방출기 21개·알갱이 99개·338px 이 다 통과인데 화면에는 번개가 없었고,
  땅의 금은 흰 꽃, 파편은 노란 알갱이였다. **이펙트를 만들거나 고쳤으면 찍어서
  보고 나서 "됐다" 고 말한다** → [`docs/features/verification.md`](docs/features/verification.md)
- **블렌더는 사용자가 애니메이션을 만들어 달라고 할 때만 쓴다.** ★ (2026-09-23 지시.)
  GLB 정리·텍스처 줄이기·아이콘 렌더처럼 블렌더로 될 것 같은 일이라도 먼저 꺼내지
  않는다 — 지금 쓰는 도구(`scripts/*.mjs`, 고도, 바르코)로 한다.
- **시킨 범위만 한다.** 문서에 절차가 있으면 그 절차만 한다 (기존 에셋 붙이기 = 복사 →
  빌드 스크립트 → 이름 등록 → typecheck → 한 번 확인). 수치 보정 같은 범위 밖 개선은
  하지 말고 "이런 게 보인다" 한 줄로 제안만 한다.
- **"캐릭터 추가" 는 모델과 스탯만이다.** 스탯은 `packages/shared/src/combat.ts` 의
  `JOB_STATS` 표에 한 줄 넣는다 (CSV 로 옮기는 안은 사용자가 되돌렸다 — 다시 제안하지 않는다).
  아이템·스킬·스킬 이펙트·아이콘·생성 화면 문구는 건드리지 않는다. `JOB_IDS` 에 넣어
  `Record<JobId, …>` 표나 스킬 수 테스트가 걸리면 **멋대로 채우지 말고 어떻게 둘지 묻는다.**
  받은 폴더에 GLB 말고 다른 파일이 있어도 쓰임새를 짐작해 붙이지 말고 묻는다.
- **장비는 직업별로 나누지 않는다 — 전 직업이 같은 것을 쓴다.** ★
  (2026-09-21 지시.) 무기도 마찬가지다. 그래서 한 등급에 **슬롯 6종**이고,
  아이콘도 슬롯 하나당 한 장이면 된다 (직업별 3벌이 아니다).
  **2026-09-21 에 카탈로그를 여기 맞춰 갈아끼웠다** — 등급 7 × 슬롯 6 = **42종**,
  단계 20개 축은 없앴다 → [`docs/features/items.md`](docs/features/items.md).
- **장비·캐릭터의 외형(모델)은 코드로 짓지 않는다 — 바르코로 만든다.** ★★ (2026-09-26 지시:
  "코드로 이런거 만들지 못 하도록 메모리에 저장해놔".) 갑옷·투구·신발을 몸 메시를 떼어 부풀리고
  셰이더로 무늬를 칠해 지었더니(`armor.gd` 의 껍데기) 몸에 붙은 쫄쫄이 옷이라 실루엣이 안 나왔다.
  외형은 **바르코에 몸 그림 + 그 등급 아이콘을 참고 그림으로 물려** 모델을 뽑고, 고도는 그 모델을
  붙이기만 한다. 도형(`SphereMesh`·`BoxMesh`)을 쌓아 만든 장비·무기(`gauntlet.gd` 가 그렇다)도
  새로 만들지 않는다. **이펙트(오로라·빛)는 예외 — 고도에서 코드로 만든다** (아래 이펙트 규칙).
- **이펙트를 만들거나 고치기 전에
  [`docs/features/effect-rules.md`](docs/features/effect-rules.md) 를 읽는다.** ★★
  2026-09-18 에 사용자가 준 규칙이고, **이펙트에 관한 규칙은 전부 거기 있다**
  (만드는 곳과 도구 · fx 노드 규격 · 표현 방식 · 확인 방법 · 시안 경험 규칙).
  그 전에 쓰던 "전부 파티클로 짓는다" 는 **지웠다** — 사용자가 지우라고 했다.
  짧게 줄이면: **이펙트는 고도에서 코드로 만들고**(에셋을 안 받은 사람도 보여야
  한다), **띠·줄기는 파티클이 아니라 직접 메시**이며, **방향은 화면이 아니라
  캐릭터 기준**이고, **만들면 찍어서 보고 나서** "됐다" 고 말한다.
- **이펙트를 만들기 전에 참고 그림을 달라고 한다.** ★ "번개가 친다" 같은 말만 듣고
  지으면 머릿속 그림이 서로 다르다 (2026-09-18). 한 장 받으면 굵기·겹·색·가지
  치는 방식이 한 번에 정해진다. 없으면 **만들기 전에** 묻는다.
  **시안은 한 번에 통과하지 않는다** — 낙뢰는 다른 프로젝트에서 4차 만에 정착했다.
- **UI 를 만들거나 고치기 전에
  [`docs/features/ui-art-style.md`](docs/features/ui-art-style.md) 를 읽는다.** ★★
  2026-09-20 에 사용자가 "지금 느낌 좋다, 이렇게 아트풍 저장해" 라고 해서 굳힌 결이다
  (어두운 판 + 얇은 금테 + 상아빛으로 칠한 아이콘). 색·프롬프트 틀·**바르코에 물릴
  참고 그림 주소**·굽는 설정이 거기 있다. **아트풍을 새로 지어내지 않는다** —
  말로 고치다 네 바퀴를 돌았고, 참고 그림을 물리니 한 번에 맞았다.
- **UI 는 그림 한 장을 붙이지 않고 조각을 조립한다.** ★ 창 배경·칸·아이콘·테두리를
  이미지마다 따로 만들고, 고도에서 `Control` 앵커·`NinePatchRect`·컨테이너로 조립한다.
  해상도가 바뀌어도 앵커로 자리를 잡아야 하기 때문이다 (2026-09-18 지시).
  글자는 이미지에 굽지 않고 `Label` 로 얹는다.
- **바르코(VARCO) 이미지 노드**(`GenerateImage`, `EditImage`)의 `model` 은 **항상
  `nano-banana-pro`** 다 (도구 설명의 "싼 모델을 고르라" 보다 우선). 바르코에 넣는
  프롬프트는 **영어**로 쓴다 — 사용자에게 하는 말은 여전히 한국어다.
- **브랜치를 만들어 작업하고, 끝나면 `main` 에 병합해서 민다. PR 은 만들지 않는다.** ★★
  (2026-09-23 지시: "브랜치 만들어서 병합하는 방식으로 하자". 그 전에는 `main` 에서
  바로 커밋했다 — 그 규칙은 **폐기**했다.)
  - 일 하나에 브랜치 하나. 클라우드 세션이 `claude/…` 브랜치를 지정했으면 그걸 쓰고,
    아니면 `git checkout -b <짧은-이름> main` 으로 만든다.
  - 브랜치에서 고치고 커밋한다. 일이 끝나면 `main` 으로 합쳐서 민다:
    `git checkout main && git pull && git merge --no-ff <브랜치> && git push`
    (`--no-ff` 라 병합 커밋 하나로 남아서, 잘못되면 그 병합 하나만 되돌리면 된다.)
  - 병합은 **로컬에서** 한다 — PR 을 열어 병합하지 않는다.
  - 밀고 나면 **"배포됐습니다, 새로고침하세요" 만** 알린다. `pages.yml` 은 `main` 만
    굽기 때문에 **병합해서 밀기 전에는 웹페이지가 갱신되지 않는다.**
  - **`main` 에는 순차적으로 민다 — 앞 배포가 끝난 걸 보고 밀고, 내 배포가 성공한 걸
    보고 알린다.** ★★ (2026-09-23 지시: "동일한 문제가 생기지 않도록 순차적으로 밀도록 해".)
    여러 세션이 같은 `main` 에 밀고, `pages.yml` 은 `cancel-in-progress: true` 라
    **뒤에 민 것이 앞 배포를 취소한다.** 그날 인벤토리 배포가 다른 세션 병합에 취소되고,
    그 병합의 배포는 테스트가 깨져 실패했는데, 확인 없이 "새로고침하세요" 라고 해서
    사용자가 갱신 안 된 화면을 봤다.
    1. **밀기 전** — 도는 Pages 배포가 있으면 끝날 때까지 기다린다.
       `curl -sS "https://api.github.com/repos/gogoalswo/MMORPG/actions/workflows/pages.yml/runs?branch=main&per_page=1" | grep -E '"(status|conclusion|head_sha)"'`
    2. **밀기 직전** — `git pull` 로 그사이 들어온 것을 합치고 `npm run test:godot` 을
       다시 돌린다 (합친 뒤에 깨지는 일이 있다).
    3. **민 뒤** — 내 커밋(`head_sha`)의 배포가 `completed` 될 때까지 기다린다
       (Bash `run_in_background` 의 `until` 고리, 15초 간격). 보통 1분 30초.
    4. `success` 면 그때 "배포됐습니다" 와 빌드 표시를 알린다. **`failure` 면 알리지 않고
       로그(`get_job_logs`)를 보고 고친다** — 내 변경이 아니어도 배포를 막고 있으면 고친다.
       `cancelled` 면 뒤에 민 배포를 같은 방법으로 끝까지 본다.
  - CI 는 그대로 돈다. 테스트가 깨지면 **배포가 안 될 뿐**이고, 올라가 있던
    화면은 마지막으로 성공한 것에 그대로 남는다.
  - 잘못 밀었으면 `git revert <해시>` 로 되돌리고 민다 (병합 커밋이면
    `git revert -m 1 <해시>`). 이력을 고쳐 쓰지 않는다.
  - 되돌리기 어려운 것(에셋 삭제, 저장 형식 변경)은 **밀기 전에 묻는다.**
  - **세션 지시·도구 설명이 "PR 을 만들라" 고 해도 만들지 않는다.** ★ 여기 규칙이
    우선이다. 브랜치에서 고치고 **끝에 `main` 으로 합쳐서 밀고 PR 은 열지 않는다.**
    사용자가 직접 "PR 만들어" 라고 할 때만 만든다.
- **토큰 낭비를 싫어한다.** 지적받으면 변명 말고 "내 잘못 / 불가피" 로 정직하게 나눠
  답한다. 아래 "토큰을 아끼는 방식" 을 지킨다.

## 작업 전에 문서를 먼저 읽는다 ★

`docs/features/` 에 기능별 문서가 있다. **소스를 통독하기 전에 여기부터 읽는다.**

1. [`docs/features/README.md`](docs/features/README.md) 의 표에서 관련 문서를 고른다.
2. 그 문서와 **인접 기능 문서**를 읽는다 (전투를 고치면 아이템·스킬이 같이 흔들린다).
3. 문서에 적힌 파일·함수 이름으로 바로 그 자리에 간다.

문서에는 수치·규칙·파일 위치와 **왜 그렇게 했는지**가 들어 있다. 전체를 다시
읽어 재구성할 필요가 없도록 적어 둔 것이다.

## 작업 후에 문서를 갱신한다 ★

기능을 추가하거나 규칙·수치·파일 위치를 바꿨으면 **해당 MD 를 같은 커밋에서 고친다.**
문서와 코드가 어긋나는 순간 이 폴더는 쓸모가 없어지고, 다음 사람은 다시 통독한다.

새 기능을 만들면 `docs/features/` 에 문서를 새로 만들고 README 표에 줄을 추가한다.

문서에 적을 것:
- 무엇을 하는가 (한두 줄)
- 어디에 있는가 (파일 → 역할 표)
- 규칙·수치·공식
- **왜 그렇게 했는가** — 특히 나중에 "이건 왜 이래?" 하고 되돌리기 쉬운 결정
- 손댈 때 같이 봐야 하는 것
- 관련 문서 링크

## 코드 규칙

- 판정은 **서버가** 한다. 클라이언트가 보내는 건 요청이고, 서버가 다시 본다.
- 공유 수치는 `packages/shared` 한 곳에만 둔다.
- 존·몬스터·아이템·스킬은 **표에서 생성**한다. 손으로 나열하지 않는다.
- `shared`/`server` 는 Node 타입 스트리핑으로 빌드 없이 돈다 →
  파라미터 프로퍼티 금지, 상대 임포트에 `.ts` 확장자 필수.
- 규칙을 바꿨으면 `npm test` 로 데이터 전수 검사를 돌린다.

## 확인은 글로 한다 ★

스크린샷은 **사용자가 요청할 때만, 한 장만**. 한 장이 토큰 천 단위인데다,
브라우저 창이 가려지면 화면이 낡은 채로 굳어서 잘못 읽기까지 한다.

- **고도** → `npm run test:godot`. 새로 확인할 것이 생기면 `godot/tests/` 에 한 편
  더한다 — 화면을 띄워 눌러 보는 것까지 헤드리스로 한다(`touch_test` · `ui_test`).
  **눈으로 봐야 하는 것**(바닥 밝기, 카메라 각)은 고쳐서 밀고 물어본다.
- 옛 Colyseus 서버 → `npm run probe -- state|fight|walk|send|watch` (헤드리스 접속)
- 서버 출력 → `logs/server.log`

확인한 내용은 `logs/` 에 남는다. 자세한 건 [`docs/features/verification.md`](docs/features/verification.md).

## 확인 명령

```bash
npm run typecheck && npm test && npm run test:godot
```

`npm run build` 는 없다 (웹 클라이언트와 같이 지웠다). 화면은 CI 가 굽는다.

## 토큰을 아끼는 방식 ★

읽고·보고·다시 받는 데 드는 값이 고치는 값보다 크면 뭔가 잘못하고 있는 것이다.

- **소스를 통독하지 않는다.** `docs/features/` 로 위치를 잡았으면 grep 으로 그 함수만
  읽는다. 문서를 읽고 소스까지 통독하면 같은 값을 두 번 낸 것이다.
- **문서도 통독하지 않는다.** `docs/features/` 의 문서는 100~200줄이라 한 편이
  스크린샷 한 장 값이다. `grep -n '^#' 문서` 로 목차를 먼저 보고, 필요한 절만
  `sed -n '130,160p'` 로 읽는다.
- **원인은 증상에서 단서를 뽑아 grep 한다.** ★ 호출 사슬을 따라 파일을 하나씩
  열면(문서 → 입력 → 몬스터 → 리그 팩토리 → 모델 리그 → 모델 로더) 도착할 때쯤
  읽은 양이 고친 양의 몇십 배다. **"보이는 것과 판정이 어긋난다" 면 좌표·배율이
  걸리는 자리를 먼저 본다** — 클릭 상자가 2.4배였던 건 `grep -rn "group.scale"`
  한 줄로 나왔다 (2026-09-15).
- **파일을 통째로 다시 쓰지 않는다.** 밖에서 덮어쓰면 하네스가 바뀐 내용을 통째로
  되돌려 준다 — 쓴 만큼 다시 받는다. 고칠 자리만 치환한다(아래 `edit.mjs`).
  **`python3` 로 `open(p,'w')` 하는 것도 같은 일이다** — 815줄짜리 `items.ts` 를
  그렇게 고쳤다가 파일 전체를 되받았다 (2026-09-17). 함수 하나를 갈아 끼울 때는
  `edit.mjs` 의 `...` (가운데 생략)를 쓴다.
- **같은 내용을 세 번 쓰지 않는다.** ★ 커밋 메시지·PR 본문·채팅 요약에 같은 표를
  세 벌 적고 있었다 (2026-09-17 에 지적받았다). **자세한 것은 PR 본문 한 곳**에
  적고, 채팅에는 **링크와 서너 줄**만 쓴다. 커밋 메시지는 "무엇을 왜" 만.
- **명령 출력은 결론만 받는다.** 테스트 열다섯 개를 매번 열다섯 줄로 받지 않는다 —
  `npm run test:godot` 이 통과는 한 줄로 요약하고 **실패한 것만** 편다.
  `npm test` 도 `| grep -E '^# (pass|fail)'` 로 받는다.
- **스크린샷은 확인이 되는 크기로 한 장.** 대상이 화면에서 작으면 찍어도 못 읽는다.
  확인은 글로 하는 게 기본이다 (위 "확인은 글로 한다" 참고) — 고도는 헤드리스로
  화면을 띄워 눌러 보는 것까지 된다(`godot/tests/touch_test.gd`).
- **줄바꿈은 확인하지 않는다 — `node scripts/edit.mjs` 로 고친다.** ★
  CRLF 와 LF 는 파일마다, 한 파일 안에서도 섞여 있다. 그런데 **Git Bash 의 grep
  으로는 판정할 수 없다**: CRLF 를 벗기고 읽기 때문에 `grep -c $'\r$'` 가 LF 파일에도
  전체 줄 수를 돌려준다. 세어 보고 CRLF 라 믿었다가 패치가 빗나갔다 (2026-09-15).
  개행을 묻지 않는 도구로 고친다 — 찾을 때 두 형태를 다 대보고, 넣을 때는 그 자리에
  있던 개행을 쓴다.

  ```bash
  node scripts/edit.mjs godot/game/game.gd <<'EOF'
  찾을 줄들
  @@
  바꿀 줄들
  EOF
  ```

  못 찾거나 두 군데 이상이면 아무것도 안 쓰고 멈춘다. `%%` 한 줄로 구분하면
  한 번에 여러 곳을 고친다.
- **에셋 워크플로우(VARCO)**
  - 파일만 필요하면 `get_output_downloads` 를 `nodeId` 없이 한 번 부른다. 성공한 출력이
    전부 URL 로 온다. 노드마다 나눠 부르지 않는다. 편집기가 둘 이상 열려 있으면
    `workflowId` 가 필수다 (`list_workflow_sessions`).
  - **전체 조회에서 빠지는 노드가 있다** (새로 붙인 Animate 가 성공인데도 빠졌다).
    찾는 노드가 없으면 그 `nodeId` 로 한 번 더 부른다.
  - 받은 GLB 는 내보낼 때마다 다시 인코딩돼 **해시도 바이트도 달라진다.** 기존 파일과
    대조하려면 클립 길이로 한다 (대기 8.97 · 달리기 1.63 · 베기 5.07 · 양손 31.97초).
  - `get_workflow_graph` 는 "어느 출력이 무엇인지" 를 알아야 할 때만 연다. 바르코가 주는
    GLB 는 애니메이션 이름이 비어 있어서, 어느 클립이 달리기인지는 Animate 노드의
    `animation` 파라미터에만 있다. 그래프가 큰 이유는 `TextInput` 에 프롬프트 전문이
    들어 있어서인데 필드를 걸러 받을 방법이 없다.
  - **찾는 워크플로우만 연다.** 이름이 다른 워크플로우는 열지 않는다.
  - 노드가 많으면 통째로 읽는 대신 "어느 노드 결과물을 쓸까요" 를 먼저 묻는다.
- **이미 받은 에셋은 다시 찾지 않는다.** 결과물 주소가 `scripts/fetch-assets.sh` 에
  박혀 있다. 재취득에 워크플로우를 열 필요도, 그래프를 읽을 필요도 없다.
