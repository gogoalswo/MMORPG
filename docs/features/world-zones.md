# 존과 월드

## 무엇

마을 1개 + 사냥터 20개 + **던전 20단계**, 전부 같은 크기의 정사각 맵. **존끼리는
이어져 있지 않다** — 어디를 가든 차원문에서 목적지를 고른다. 던전만은 차원문 목록에
없고 가방 옆 던전 창으로 들어간다 → [dungeons.md](dungeons.md).

## 어디

| 파일 | 역할 |
|---|---|
| `packages/shared/src/zones.ts` | 존 정의 21개 (**생성 코드**, 손으로 안 적는다) |
| `packages/shared/src/zone.ts` | `ZoneDef` 타입, 환경(하늘·안개·풀) 타입 |
| `packages/shared/src/zones.test.ts` | 차원문 유무·자리, 도달 가능성, 레벨 순서 전수 검사 |
| `packages/client/src/scene/zoneScene.ts` | 존 하나를 3D로 세운다 |
| `packages/client/src/scene/{ground,sky,portal,lighting}.ts` | 지면·하늘·차원문·광원 |
| `packages/client/src/scene/ground.ts` | **바닥 셰이더** — 텍스처 종류별 `LOOKS`(타일 크기·섞기·러프니스·평균색·발광), 존 틴트 |
| `packages/client/src/scene/assets.ts` | 바닥 7종(색+노멀 KTX2)을 부팅 때 전부 받는다 |
| `godot/game/game.gd` | `environment_for` — 존 분위기(하늘·환경광, **안개 없음**) |
| `godot/game/ground.gd` | **고도 바닥 재질** — 틴트·대비·노멀 세기·타일 반복을 셰이더에 넣는다 |
| `godot/game/ground.gdshader` | 바닥 셰이더 — 아니소트로픽 필터, 무늬 대비, 노멀 세기 |
| `godot/game/terrain.gd` | ★ **지형** — 존별 높낮이 메시 + 바닥 넉 장 섞기 그림. `RECIPES` 에 적힌 존만(지금은 마을). `height_at` 이 발밑 높이 |
| `godot/game/terrain.gdshader` | 지형 셰이더 — 풀·돌판·자갈·흙을 섞기 그림대로, 비탈은 흙으로 |
| `godot/tests/terrain_test.gd` | 광장 평평함 · 걷는 땅 굴곡 · 언덕이 시선을 안 가리는지 · 발 높이 = 메시 · 섞기 그림 끝이 풀인지 |
| `scripts/build-ground-textures.mjs` | 바닥 이미지 7장 → 512² 색 + 밝기로 만든 노멀 JPEG (→ `npm run compress`) |
| `packages/client/src/ui/zoneGate.ts` | 차원문 창 — 마을·사냥터를 골라 바로 간다 |
| `packages/server/src/ZoneRoom.ts` | 존 하나 = Colyseus 룸 하나 |
| `scratchpad/gen_zones.py` | zones.ts 를 뽑아내는 생성기 (세션 스크래치패드) |

## 규칙

### 크기
- 모든 존 `size: 66`, 이동 가능 영역 ±29.
- **2026-09-23 에 92 → 62 (2/3)로 줄였다** ("맵이 너무 커"). 92 는 FOV 30 / 거리 40 /
  부각 42 에서 16:9 화면에 보이는 지면(약 56.5유닛)의 1.5배였다.
- **같은 날 62 → 66 으로 조금 되돌렸다.** "몬스터가 포탈이랑 너무 가깝다" 해서 무리를
  ±14 → ±16 으로 밀었는데, 무리가 이미 벽에 닿아 있어 맵을 키우는 것 말고는 자리가
  없었다 (마릿수를 줄이는 안과 벽에 걸치는 안 중 사용자가 이것을 골랐다).
- 무리·보스·차원문 자리가 같이 당겨졌다 (아래 각 절). 줄이면서 **도착 지점이 무리
  인식 범위 밖**과 **자동 사냥이 옆 무리를 안 끈다** 두 규칙을 놓았다 — 50마리를
  유지하면서 둘 다 지킬 자리가 없어서, 사용자가 "무리를 붙인다" 를 골랐다.
- 도착 지점은 어느 존이든 `default` = 맵 한가운데 `(0, 0)` 하나뿐이다.

### 차원문 ★
```
마을 ─┐
       ├─ 차원문 ─┬─ 초원(1-10)
사냥터 ┘          ├─ 덤불숲(10-20)
                  ├─ …
                  └─ 종말의 대지(190-200)
```
**모든 존의 `(4, 0)` 에** 목적지를 고르는 문이 하나 있다 (`ZoneDef.gate`,
`GateDef`). 밟으면 **이동하지 않고 목록을 연다.** 고르면 그 존의 `default`
스폰(맵 한가운데)으로 간다.

- **걸어 들어가는 포탈은 없다.** 예전에는 존끼리 사슬 포탈로 이어져 있어서
  가장자리(±34)를 밟으면 곧장 옆 사냥터로 넘어갔다. 전부 없앴다(요청).
  `zones.test.ts` 가 `ZoneDef` 에 `portals` 가 되살아나지 않는지 검사한다.
- **그래서 문은 모든 존에 있어야 한다.** 하나라도 빠지면 그 존은 들어가면
  죽는 것 말고는 나올 방법이 없는 방이 된다. 마을에만 두던 예전 규칙은
  사슬이 있을 때 이야기였다. `zones.test.ts` 가 검사한다.
- **목록 맨 위가 마을이다.** 돌아가는 길이 20줄 끝에 있으면 매번 훑어야 한다.
- 자리·색·이름을 **존마다 같게** 둔다 (`GATE_SPOT` `GATE_COLOR` `GATE_NAME`).
  존을 옮길 때마다 "나가려면 어디로" 를 다시 찾지 않는다. 이것도 검사한다.
- 색은 파랑 `#4aa8ff`, 문 위에 **「사냥터 이동」** 이 파란 글씨로 뜬다
  (`NameplateLayer` 의 `kind: 'portal'`). 밟기 전에는 무슨 문인지 알 수 없어
  그냥 지나치게 된다. 차원문 **창**도 같은 파란색으로 맞춰 뒀다.
- `test()` 의 armed 규칙 — 반경을 한 번 벗어나야 다시 발동하므로,
  창을 닫고 문 위에 서 있어도 계속 다시 열리지 않는다.
- 지금 서 있는 존은 목록에서 `현재` 로 표시하고 누를 수 없게 한다. 눌러 봐야
  존을 다시 세우기만 하고 아무 데도 안 간다.
- 자리 규칙: 스폰·NPC·몬스터 무리와 겹치면 안 된다. 겹치면 예외가 아니라
  "접속하자마자 창이 열린다" 같은 증상으로만 나타나서 `zones.test.ts` 가 잡는다.
  **도착 지점과 달리 인식 범위(`aggroRange`)까지는 안 본다** — 문은 걸어서 가는
  자리라 그렇게 잡으면 네 무리(±16) 사이에 놓을 데가 없다.
- **(9, 0) → (4, 0)** (2026-09-23). 무리가 ±14 로 붙으며 9 에서는 동쪽 두 무리
  가장자리에 문이 걸쳤다. 무리를 ±16 으로 민 지금은 x ≤ 10.2 면 검사를 지키지만
  **4 에 그대로 둔다** — 문과 몬스터 사이를 벌리려고 민 것이라서다 (무리 가장자리까지 7m).

**잠그지 않는다.** 레벨대(`Lv.33~38 보스 39`)와 위험/적정/쉬움만 표시하고 아무
사냥터나 고를 수 있다. 근거가 둘이다 — (1) 존에 들어가는 것 자체는 **원래도**
서버가 막지 않는다. 클라이언트가 룸을 고르고 서버는 받아준다. 여기서만 막으면
걸어가는 길로 우회하면 그만이라 규칙이 아니라 불편이 된다. (2) 파워레벨링은
경험치 감쇠가 이미 막고 있다 ([monsters-progression.md](monsters-progression.md)).
정말로 막아야 한다면 `ZoneRoom.onJoin` 에 검사를 넣어야 하고, 그건 이미 그 존에
서 있는 캐릭터를 접속 불가로 만들 수 있어 따로 결정할 일이다.

판정 기준은 `REACH = 4` 하나다 — `combat.test.ts` 가 레벨 곡선을 검사할 때
쓰는 값과 같은 눈금이라, 화면에 "적정" 인데 경험치는 안 붙는 구간이 안 생긴다.

### 무리 자리
- `PACKS = [(-14,-14), (14,-14), (-14,14), (14,14)]` — 네 귀퉁이, `radius: 13`.
- **±28 → ±14 → ±16** (2026-09-23). 맵을 2/3 로 줄이며 ±14 로 당겼다가, "포탈이랑 너무
  가깝다" 해서 맵을 66 으로 키우고 ±16 으로 밀었다. 무리 바깥 끝 16 + 13 = 29 가 이동
  가능 영역 끝과 같아 **맵을 키우지 않고는 더 밖으로 못 민다.** 무리 간격 32 라 옆 무리
  원과 6m 떨어져 있다.
  - 도착 지점(0,0)에서 무리 중심까지 22.6 — 반경 13 + 인식 범위(최대 16) 안이다.
    **사냥터에 들어서면 몬스터가 달려온다.** 받아들인 것이고, `zones.test.ts` 는
    이제 "도착 지점이 무리 **원** 안은 아니다" 만 본다.
  - 자동 사냥 반경 27 이 옆 무리까지 닿는다 ([auto-hunt-and-targeting.md](auto-hunt-and-targeting.md)).
  - 사냥터에서 이벤트를 직접 넣거나 고르기만 보는 고도 테스트(`hit_fx_test` · `select_test`)는 몬스터의
    `aggro` 를 0 으로 꺼 두고, 땅을 누르는 `ui_test` 는 몬스터 없는 자리를 골라 누른다.
  앞 둘은 약한 종(`tier[0]`), 뒤 둘은 강한 종(`tier[1]`).
- **네 무리 전부 `count: 50`, `respawnMs: 10000`(10초).** 2026-09-18 요청.
  한 존에 **201마리**(무리 200 + 보스 1)가 선다.
- **50마리인 이유**는 [stat-balance.md](stat-balance.md) 의 단위가 "한 그룹 50마리" 이기
  때문이다 — 범위 스킬로 그 50마리를 15초에 정리하고 HP 50% 를 잃는 것이 전 구간의
  기준이다. 무리 하나 = 한 그룹이라야 문서의 수치를 게임에서 그대로 잰다.
- (이전 기록) **반경 8 → 13, 자리 ±20 → ±28** 은 50마리를 넣으면서 따라온 값이다.
  - 반경: 한 마리가 차지하는 넓이를 그대로 두려면 √(50/20) = 1.58 배 → 8 × 1.58 ≈ 13.
  - 자리: ±20(중심에서 28.3)이면 도착 지점이 **무리 반경 13 + 인식 범위 16 = 29** 안에
    들어가 사냥터에 들어서자마자 50마리가 달려들었다. 또 무리 간격 40 으로는 자동 사냥
    반경이 "무리 하나는 통째로(≥26) · 옆 무리는 안(≤22)" 을 **동시에 만족할 수 없다.**
    ±28 이면 도착 지점까지 39.6, 무리 간격 56 이라 둘 다 풀린다.
- 마릿수를 더 늘리거나 반경을 줄이면 `scatterSpawn` 이 빈 자리를 못 찾는다 —
  `zones.test.ts` 의 "무리 안에서 몬스터가 서로 겹치지 않는다" 가 전수로 잡는다
  ([collision.md](collision.md)). 반경 13 · 50마리는 **가장 가까운 둘 사이 여유 0.208m**
  로 통과하지만, 반경 8 · 20마리(0.213m)와 마찬가지로 여유가 많지 않다.

### 보스 자리
- `BOSS_SPOT = [0, 23]` — 중심에서 23유닛, **6시 방향**. 모든 사냥터 동일.
- 원래 7시·24유닛. 맵을 줄이며 무리가 네 귀퉁이를 덮어 무리 원 밖으로 남는 자리가
  두 무리 사이 축 위뿐이다. 도착 지점에서 23 이라 보스 인식 범위(3 + 18 = 21) 밖이다.
- `respawnMs: 900000` (15분), `count: 1`, `radius: 3`.

### 바닥 ★
바닥은 **이미지 7장**(바르코로 만든 타일 텍스처, [ASSETS.md](../ASSETS.md))으로 깐다.
존마다 `env.ground` **한 장만** 깐다. 풀 잎·흙길·자갈길은 없다.

| 바닥 | 존 |
|---|---|
| `stone` 돌판 | 마을 |
| `grass` 풀 | 초원·덤불숲·안개 늪·검은 삼림·뒤틀린 숲·빛바랜 고원 |
| `dirt` 마른 땅 | 메마른 협곡·잿빛 황야·소금 평원·그림자 계곡 |
| `snow` 눈 | 서리 고원·얼어붙은 심연 |
| `sand` 모래 | 붉은 사막·유황 분지 |
| `cobble` 자갈 | 무너진 성터·폐허 도시·심연의 문턱 |
| `lava` 용암 균열 | 화산재 언덕·균열 지대·종말의 대지 |

- **사냥터 20곳이 여섯 장을 나눠 쓴다.** 이미지가 7장이라고 존을 7개로 줄이지 않았다 —
  몬스터 티어·아이템 단계(20)·레벨 곡선이 사냥터 수에 1:1 로 묶여 있다 (2026-09-10
  요청: 사냥터 20개 유지, 바닥만 교체).
- **같은 텍스처를 쓰는 곳끼리는 존 색으로 갈린다.** `groundTint`(풀밭은 `grassLight`,
  나머지는 `dirtLight`) 쪽으로 텍스처 평균색을 절반(`TINT_PULL` 0.5)만큼 끌어당긴다.
  1 이면 소금 평원이 마른 땅의 제 색을 완전히 잃고, 0 이면 초원과 검은 삼림이 같은
  바닥이 된다. 명암(무늬)은 곱셈이라 그대로 남는다. 소금 평원은 금 간 마른 땅을
  하얗게 물들인 것이다.
- **반사율을 0.3배로 깐다**(`ALBEDO`). 조명(태양 2.7·환경광·ACES)은 예전 ambientCG
  텍스처 — 어둡고 존 색 ×2 를 곱해 쓰던 — 에 맞춰져 있다. 바르코 이미지는 화면에 보일
  밝기로 그려져 있어서 그대로 깔면 눈·모래·소금 평원이 하얗게 날아가 무늬가 사라졌다.
  마을에서 재 보니 반사율 → 화면(선형)이 3.2배였다. 조명을 건드리면 캐릭터·몬스터가
  같이 흔들리므로 바닥 쪽을 낮췄다.
- **풀 잎·길은 없다** (2026-09-10 요청: 이미지 준 맵만 깐다). 예전에는 풀밭·마을에
  흙길·자갈길을 스플랫 마스크로 섞고(`ROAD_FOR`, `createPathMask`), 풀 잎을 인스턴싱으로
  수만 포기 심었다(`GRASS_SHARE`, `grass.ts`). 셋 다 코드째 뺐다. `zones.test.ts` 가
  `road`·`roads`·`grassCount` 가 `env` 에 돌아오지 않는지 검사한다.
  **마을의 자갈길은 이것과 다르다** — 2026-09-26 에 지형을 만들라고 해서 고도 쪽
  `terrain.gd` 가 바닥 넉 장을 섞어 깐다(아래 "지형"). shared `env` 에는 여전히 길이 없다.
- `grassDark`/`grassLight` 는 이름만 풀이다 — 하늘·반구광의 땅 쪽 색과 풀밭 틴트로 계속 쓴다.
- 텍스처 성질은 존이 아니라 **이미지에 딸린 것**이라 `ground.ts` 의 `LOOKS` 에 있다.
  - 타일 크기: 돌판 6m(판석 80cm), 자갈 8m, 용암 12m, 나머지 4~6m. 자갈을 4m 로 두면
    기본 카메라에서 알이 안 보이고 잡티가 되고, 용암을 8m 로 두면 재 얼룩이 격자로 늘어선다.
  - `blend`: 배율이 다른 샘플을 겹쳐 반복을 깨는 비율. 돌판·자갈·용암처럼 **모양이
    뚜렷한 것은 0** — 섞으면 판석이 두 겹으로 비친다.
  - `glow`: 용암 균열은 스스로 빛난다. **틴트 전 원래 색**에서 붉고 파랑이 빠진 곳만
    골라서, 어두운 존 색을 곱해도 균열 빛은 안 죽는다.
  - `mean`: 텍스처 평균색. 틴트의 기준이라 **빌드 스크립트가 출력하는 값과 같아야 한다.**
- 이미지에 노멀·러프니스 맵이 없다. 노멀은 빌드 때 **밝기를 높이로 봐서** 만들고
  (`build-ground-textures.mjs`, 이음새가 안 생기게 반대편 가장자리와 이어서 계산),
  러프니스는 종류별 상수다.
- **고도에는 안개가 없다** ★ (2026-09-18 요청: "안개를 넣으라고 한 적이 없는데
  왜 넣은거야? 그냥 안개를 없애버려"). `environment_for` 가 `fog_enabled = false`
  로 두고, `ground_test.gd` 가 존 세 곳에서 꺼져 있는지 검사한다.
  - 안개는 옛 웹 클라이언트에 있던 것이 **고도 이관 때 따라온 것**이다(`e4a3791`).
    옮기면서 선형(70m → 190m)이 near 없는 지수 안개(`fog_density` 0.006)로
    바뀌어, 70m 앞에서나 끼어야 할 것이 **카메라 바로 앞(27m) 바닥에도 15%
    섞이고 있었다.**
  - 안개는 곱이 아니라 **더하기**라 돌 틈처럼 어두운 데를 그대로 들어올린다 —
    돌판 틈과 판면이 화면(sRGB)에서 0.15 대 0.37 이던 것이 0.34 대 0.45 가 돼
    무늬가 거의 사라지고, 화면이 안개색(`#c2c8b8`)으로 떴다.
  - 조명(태양·환경광)은 반사율에 **곱해지므로** 무늬 대비 비율을 안 건드린다.
    대비를 깨는 건 더하기로 들어오는 안개뿐이었다.
  - **데이터에서도 지웠다.** `ZoneEnv` 의 `fogColor`·`fogNear`·`fogFar`, 테마 표
    (`FIELD_THEMES`)의 `fog` 열, `envFor` 의 안개 보간이 전부 없다. 옛 Colyseus
    서버는 `env` 를 그대로 넘겨주기만 해서 따로 고칠 데가 없었다.
- **고도는 바닥을 셰이더로 깐다**(`godot/game/ground.gdshader`). 2026-09-17 에
  "이미지로는 재질이 잘 보이는데 깔아 놓으면 돌 틈·풀이 너무 희미하다" 는 지적을
  받아 `StandardMaterial3D` 에서 옮겼다. 표준 재질로는 아래 셋 중 어느 것도 못 준다.
  - **무늬 대비**(`CONTRAST` 1.4) — 텍스처 평균색을 축으로 밝기 **비율**을 지수로
    벌린다(`mean * pow(c / mean, 1.4)`). 평균이 그대로라 존 밝기는 안 변하고 무늬만
    또렷해진다. 빼기로 벌리면 어두운 쪽이 0 에 붙어 돌 틈이 새까만 선이 된다.
    돌판 텍스처는 밝기 폭이 6.4배 → 13.4배가 된다 (`ground_test.gd` 가 찍는다).
    반사율 배율(`ALBEDO`)로는 안 된다 — 곱셈이라 밝기만 낮추고 대비는 그대로다.
  - **아니소트로픽 필터** — 쿼터뷰(내려보기 43도)라 바닥이 비스듬하다. 밉맵만 쓰면
    세로로 눌린 만큼을 기준으로 단계가 잡혀서 가로 무늬까지 같이 뭉갠다.
    표준 재질의 기본 필터에는 아니소트로픽이 없다.
  - **노멀 세기**(`NORMAL_DEPTH` 2.0) — 밝기로 만든 노멀맵이라 기본 1.0 으로는
    요철이 밋밋하다.
- 텍스처 자체에는 무늬가 충분히 있다(2026-09-17 에 잰 선형 밝기 — 돌판 p5 0.016 ·
  p95 0.102, 자갈 0.036 · 0.329). 화면에서 희미했던 건 이미지가 아니라 재질 쪽이었다.
- 일곱 장을 **부팅 때 전부** 받는다(KTX2 14장 2.5MB). 존마다 받으면 존 구성이
  비동기가 되는데, 그만한 크기가 아니다.

### 지형 ★ (2026-09-26 — 마을부터)
"맵 장애물은 넣지말고 지형 만들어 봐. 마을부터" 에 따라 **높낮이와 바닥 섞기만** 넣었다.
나무·바위·건물·벽처럼 길을 막는 것은 하나도 없다. 존마다 `terrain.gd` 의 `RECIPES` 에
한 줄로 적고, 없는 존(지금은 사냥터 20곳·던전)은 예전처럼 평평한 한 장(`Ground`)이다.

**마을 생김새**

| | 값 | 왜 |
|---|---|---|
| 돌판 광장 | 반지름 11m, 가장자리를 노이즈로 흩뜨림 | NPC 6명·차원문·도착 지점이 다 여기 선다. **완전히 평평**(높이 0) — 비스듬한 땅에 모델을 세우면 한 발이 묻힌다 |
| 자갈길 | 네 갈래(동서남북으로 굽이치는 꺾은선), 반폭 1.7m | 광장 안에서 시작해 바깥 언덕을 오르다 풀 속으로 사라진다 |
| 흙 | 길섶 · 광장 둘레 · 군데군데 맨땅, 그리고 **비탈**(법선 y 0.92→0.8) | 풀 한 장이면 넓은 땅이 밋밋하다 |
| 풀 | 나머지 전부 | |
| 걷는 땅 굴곡 | ±0.7m(실측 −0.47~0.46), 기울기 0.24 이하 | 광장 둘레 1→7m 에서 스며들고, 길 위는 0.4배로 눌렀다 |
| 바깥 언덕 | 이동 끝(±29)에서 0 으로 시작해 메시 끝(±64)에서 12m | 맵 끝에서 바닥이 끊겨 하늘색이 비치지 않고, 마을이 골짜기에 앉은 것처럼 보인다 |

- **높이는 그리기만 한다.** 판정(`World`)은 여전히 평면 (x, z) 에서 돈다 — 서버에 알릴
  것이 없다. 화면에서 높이를 쓰는 곳은 전부 `game.gd` 의 `_ground_y(x, z)` 한 곳을 거친다
  (나·몬스터·NPC·이름표·차원문·고리·체력 막대·스킬 이펙트 자리·범위 예고 원).
  `HpBar3D.follow`·`SelectRing.follow` 는 `at.y` 를 발밑 높이로 받는다.
- **발 높이는 메시와 같은 삼각형으로 보간한다**(`height_at`). 노이즈를 다시 재면 칸
  한가운데서 발이 몇 cm 뜨거나 묻힌다. 테스트가 꼭짓점과 삼각형 안쪽을 대조한다.
- **화면을 누른 곳**은 `ray_hit` — 높이 0 평면에서 시작해 그 자리 높이의 평면으로 몇 번
  다시 맞춘다. 걷는 땅 기울기(0.24)가 시선(42도)보다 한참 눕기 때문에 금방 모인다.
- **이동 끝 너머를 누르면 끝으로 당긴다**(`game.gd` 의 `_set_target` — 누를 때도 끌 때도, ±`_half_size`).
  언덕이 끝 너머까지 그려져서 거기를 누를 수 있는데, World 는 끝에서 멈추니 목표에 영영
  못 닿아 **제자리 뛰기**를 했다(2026-09-26). 고리도 당긴 자리에 선다. `touch_test` 가 본다.
- **언덕은 카메라 시선보다 가파르면 안 된다.** 끝에 선 캐릭터 앞(카메라 쪽) 언덕이
  시선 기울기(tan 42° ≈ 0.9)보다 가파르면 캐릭터를 가린다. 처음 수치(끝 14m, 노이즈
  ±2.4m)는 1.27 이었다 → 12m · ±1m 로 눌러 0.9 아래로 맞췄다. 테스트가 잰다.
- **섞기 그림**(RGB = 돌판·자갈·흙, 풀은 나머지)은 ±40m 를 1m 에 3칸으로 굽는다. 그림
  밖은 가장자리 칸이 늘어나므로(clamp) **가장자리는 반드시 풀**이어야 한다 — 아니면
  줄무늬가 무한히 뻗는다. 언덕 쪽 31~37m 에서 길·흙을 걷어 내고, 테스트가 잰다.
- 틴트: 풀은 `grassLight`, 돌판·자갈은 `groundTint` 쪽으로 끌어당긴다(사냥터 바닥과 같은
  규칙). **흙은 제 색 그대로** — `groundTint` 가 회색이라 끌어당기면 잿빛 돌처럼 된다.
  대비·노멀 세기·반사율은 `ground.gd` 의 상수를 그대로 쓴다.
- **존마다 한 번만 짓는다**(`Terrain._built`). PC 에서 섞기 그림까지 0.26초라 폰에서는
  마을을 드나들 때마다 멈칫한다.
- 메시는 1m 격자 ±64m(129² 꼭짓점, 삼각형 3.3만)다.

**사냥터에 넣을 때**: `RECIPES` 에 한 줄 더하고 층(`LAYERS`)이 그 존 바닥과 맞는지 본다.
높이를 쓰는 자리는 `_ground_y` 로 이미 다 걸려 있지만, 이펙트 파일(`*_fx.gd`)이 받은
자리 **아래로** 땅을 그리는 것(금·파편)은 평평한 땅을 가정한다 — 비탈에서는 반쯤
묻힐 수 있으니 찍어서 본다. 범위 예고 원도 평평한 판이라 비탈에서 한쪽이 묻힌다.

### 그 외
- **프랍(나무·바위)은 없다.** 전부 제거했다. 길을 막는 것은 지금도 없다 — 마을은
  높낮이와 바닥 섞기만 있다(위 "지형").
- 마을은 안전지대(몬스터 스폰 없음). `zones.test.ts` 가 검사한다.
- 죽으면 마을로 보낸다(`deathOverlay.onReturn`). 차원문과 **같은 `travel`** 을
  쓴다 — 룸을 나가고 들어가는 순서를 두 벌 만들면 한쪽만 고치게 된다.
- `resolveZoneId(id)` — 없어진 존 id 가 저장돼 있으면 시작 존으로 떨어뜨린다.
  존을 지워도 캐릭터가 접속 불가가 되면 안 된다.

## 손댈 때

- 존을 추가/수정하려면 `zones.ts` 를 직접 고치기보다 생성 규칙을 고친다.
- 사냥터를 추가하면 차원문 목록(`FIELD_ORDER`)에 **저절로 들어간다.** 창은
  `zones.ts` 에서 이름·레벨대를 읽어 만들므로 따로 고칠 데가 없다.
  단 **`gate` 를 빼먹지 말 것** — 빼면 못 나오는 방이 된다.
- 고친 뒤 `npm test` — 문이 빠지거나 겹치면 여기서 잡힌다. 눈으로는 못 잡는다.
- **바닥 이미지를 바꾸거나 더하면**: 원본을 `assets-src/textures/varco/` 에 두고
  `build-ground-textures.mjs` 의 `SOURCES` 에 적는다 → 스크립트 → `npm run compress` →
  같은 이름을 `GROUND_KINDS`(shared `zone.ts`)와 `LOOKS`(client `ground.ts`, 출력된
  평균색까지)에 넣는다. 아무 존도 안 쓰는 이미지는 `zones.test.ts` 가 잡는다.
- 바닥은 **눈으로 봐야 한다.** 캐릭터 생성 화면이 월드를 가리므로, 콘솔에서
  `loadAssets` · `buildZoneScene` · `setupLighting` 을 불러 존 여러 곳을 오프스크린
  렌더러로 그려 한 장에 모아 본다(2026-09-10 에 9곳을 이렇게 봤다). 셰이더 오류는
  `renderer.debug.onShaderError` 로 글로 받는다.
- 존 개수를 바꾸면 [monsters-progression.md](monsters-progression.md) 의 티어 수,
  [items.md](items.md) 의 단계 접두어 20개가 같이 흔들린다.

## 관련

- [godot-migration.md](godot-migration.md) — 고도 클라이언트도 같은 `.ktx2` 14장과
  같은 틴트 식을 쓴다. 이 때문에 텍스처 성질 표(`LOOKS`)가 `ground.ts` 에서
  `shared/zone.ts` 의 `GROUND_LOOKS` 로 옮겨졌다. `blend`·`glow` 는 고도 쪽에 아직 없다

[monsters-progression.md](monsters-progression.md) · [networking-state.md](networking-state.md)
